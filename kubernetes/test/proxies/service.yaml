apiVersion: v1
kind: Service
metadata:
  name: develop-service-proxies
  namespace: nm-develop
spec:
  selector:
    app: proxies-test
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
